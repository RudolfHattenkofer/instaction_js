= instaction.js

Lets you register selectors for elements with a callback that is run every time a matching DOM element is added to the pag . Useful for replacing <select> fields or other progressive-enhancements!
